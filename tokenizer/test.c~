#include <stdio.h>
#include <stdlib.h>
#include "mytoc.h"
#include "test.h"

int main(){

  /*
  char* str;
  char delim;

  str = "hello world";
  delim = '\0';
  
  //  mytoc(str, delim);

  int try;
  try = countWords(str);

  printf("%d",try);
  */
  char * str = "Hello World";
  char delim = ' ';
  //int s = countWords(str, delim);
  //printf("%d\n", s);
  
  char ** vector = mytoc(str, delim);
  for(int i = 0; vector[i]; i++)
    printf("\t\t: %s \n", vector[i]);
  
 return 0;
}
